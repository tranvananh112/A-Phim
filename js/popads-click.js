// PopAds Click Trigger - KHÔNG SỬ DỤNG
// File này đã bị vô hiệu hóa vì PopAds tự động trigger, không cần click
// Giữ lại để tham khảo

(function () {
    'use strict';

    // DISABLED - PopAds tự động hoạt động tốt hơn
    const CONFIG = {
        enabled: false, // TẮT - không sử dụng
        minTimeBetweenPops: 60000,
        maxPopsPerSession: 3,
        triggerSelectors: []
    };

    console.log('[PopAds Click] DISABLED - PopAds auto-trigger is better');

})();
